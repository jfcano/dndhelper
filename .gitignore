.venv/
__pycache__/
*.pyc
.pytest_cache/

# Ficheros locales del backend (manifiestos de ingesta, imágenes de mundos, etc.).
# Los embeddings viven en PostgreSQL (pgvector), no aquí.
backend/storage/

# PDFs locales (opcional, depende de si quieres commitearlos)
backend/data/

# Datos extra locales (no versionar)
extradata/

# Variables de entorno (claves secretas)
.env

# Scripts locales para publicar imágenes (no hace falta versionarlos)
scripts/dockerhub-build-push.sh

# Archivos del sistema/editor
.DS_Store
.vscode/
.idea/

# Materiales locales de presentacion
presentacion/
