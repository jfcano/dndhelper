FROM mcr.microsoft.com/playwright:v1.58.2-noble

WORKDIR /work

COPY package.json package-lock.json ./
RUN npm ci && npx playwright install chromium

COPY . .

ENTRYPOINT ["/bin/bash", "/work/docker-e2e-entrypoint.sh"]
